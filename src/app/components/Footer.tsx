const Footer = () => {
     return (
          <footer className="flex justify-center items-center py-6 text-sm opacity-30 sticky bottom-0">
               FOOTER
          </footer>
     );
};

export default Footer;
